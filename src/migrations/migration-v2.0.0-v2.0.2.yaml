# Default values for kubernetes-agent

agent:
  space: "Default"
  # -- The target environments to register the agent with
  # @section -- Agent values
  deploymentTarget:
    final:
      environments: {{ join "," .agent.deploymentTarget.initial.environments | quote }}
  